# Rollback (Demo)

To roll back:

1. Identify the previous GitHub Release (e.g. v0.9.3).
2. Use the bundle from that release when building a new Citrix master image,
   or revert to the previous image snapshot.
3. Never copy random DLLs from developer machines into production.
